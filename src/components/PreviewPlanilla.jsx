import Logo from './Logo'

export default function PreviewPlanilla({ roster, event, club, onBack, onConfirm, sending }) {
  return <main className="mx-auto max-w-5xl p-4 sm:p-8"><div id="print-plan" className="card overflow-hidden bg-white p-5 sm:p-8"><div className="mb-6 flex items-start justify-between gap-4"><div><h1 className="text-2xl font-bold text-brand-800">Planilla de inscripción</h1><p>{event.name}</p><p className="font-semibold">{club.name}</p></div><Logo className="size-20" variant="color" /></div><div className="overflow-x-auto"><table className="w-full min-w-[700px] border-collapse text-left text-sm"><thead><tr className="bg-brand-50 text-brand-800">{['#','Apellido','Nombre','Sexo','Edad','Evento','Tiempo'].map(label => <th key={label} className="border p-2">{label}</th>)}</tr></thead><tbody>{roster.flatMap((athlete, athleteIndex) => athlete.events.map((eventItem, eventIndex) => <tr key={`${athlete.id}-${eventItem.eventIndex}`}><td className="border p-2">{eventIndex ? '' : athleteIndex + 1}</td><td className="border p-2">{eventIndex ? '' : athlete.lastName}</td><td className="border p-2">{eventIndex ? '' : athlete.firstName}</td><td className="border p-2">{eventIndex ? '' : athlete.sex}</td><td className="border p-2">{eventIndex ? '' : athlete.age}</td><td className="border p-2">{eventItem.label}</td><td className="border p-2 font-mono">{eventItem.time}</td></tr>))}</tbody></table></div></div><div className="no-print mt-5 flex flex-wrap justify-end gap-2"><button className="btn-secondary" onClick={onBack}>Volver y editar</button><button className="btn-secondary" onClick={() => window.print()}>Imprimir planilla</button><button className="btn-primary" onClick={onConfirm} disabled={sending}>{sending ? 'Enviando…' : 'Confirmar y enviar'}</button></div></main>
}
